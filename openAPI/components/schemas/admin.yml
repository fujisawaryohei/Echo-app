openapi: "3.0.3"

info:
  title: "Admin Schema"
  description: |
    - Admin Schema
  version: "1.0.0"

paths: {}

components:
  schemas:
    Admin:
      type: object
      properties:
        id:
          type: integer
        name:
          type: string
        email:
          type: string
        password:
          type: string
        password_confirmation:
          type: string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string 
          format: date-time

    AdminIndex:
      type: array
      items:
        type: object
        properties:
          id:
            type: integer
          name:
            type: string
          email:
            type: string
          created_at:
            type: string
            format: date-time
          updated_at:
            type: string 
            format: date-time